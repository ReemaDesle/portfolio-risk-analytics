user mode:
1.take input for the types of stocks ticker  from dropdown (this dropdown should be populated from the tickers we have scraped) and number of stocks and date ( date to be chosen only from the time window we have scraped, the date will decide what data to be used for the analysis like present date and the dates before it)
2. on basis of stocks and weighatge a type of portfolio from the 4 is assigned by us(this can be simple rule based on the basis of dictionary mapping of tickers and portfolio type of each with a weight according to their quantity of stocks)
3.recommended course of action + justification (using xai- options for short summary and detailed using graphs keep this using toggle),line graph for tickers
4.recovery justification
5. map how every ml model is helping out the user ( this will be visible only if the user chooses the toggle of detailed reasoning xai )

technical mode:
1.arima
2.6 ml models hypothesis eda statistical analysis mapping to the models
3. placing of power bi graphs and decide what graphs to create from the analysis and datsets

